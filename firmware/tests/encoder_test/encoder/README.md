# MT6701 Encoder Driver (Test Copy)

This is a copy of the MT6701 encoder driver for the encoder_test sketch.

**Main implementation**: `firmware/ESP32_MCU_Firmware/encoder/`

This copy ensures the test sketch is self-contained and can be compiled independently.
